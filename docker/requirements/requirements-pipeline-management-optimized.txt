# Optimized requirements for Pipeline Management Service
# Only includes essential packages for pipeline timing and orchestration

# Core data handling
pandas>=2.0.0

# Database connectivity (lightweight)
psycopg2-binary>=2.9.0

# Configuration and scheduling
python-dateutil>=2.8.0
pytz>=2023.3

# Logging and monitoring
structlog>=23.0.0

# JSON handling (built-in json module is sufficient for most cases)
# HTTP requests for API calls
requests>=2.31.0

# Environment variable handling
python-dotenv>=1.0.0

# ML packages for Phase 5 & 6 (lightweight versions)
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0

# System monitoring
psutil>=5.9.0

# API packages for prediction service
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# Web framework packages (lightweight)
flask>=3.0.0
flask-cors>=4.0.0

# Total packages: ~15 (vs 50+ in full requirements.txt)
# Estimated build time: 5-7 minutes (vs 15-20 minutes)
