# Optimized requirements for React/Web App Service
# Only includes essential packages for API and web serving

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Data handling for API responses
pandas>=2.0.0

# Database connectivity for API queries
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# CORS for web app
fastapi-cors>=0.0.6

# Configuration
python-dotenv>=1.0.0

# JSON handling for API responses
pydantic>=2.0.0

# Logging
structlog>=23.0.0

# Total packages: ~10 (vs 50+ in full requirements.txt)
# Estimated build time: 3-4 minutes (vs 15-20 minutes)
